pre-commit:
  scripts:
    cobra-docs:
      runner: bash
    verify:
      tags: default
prepare-commit-msg:
  scripts:
    jira-ticket-info:
      runner: ruby
commit-msg:
  scripts:
    template-checker:
      tags: default
pre-push:
  scripts:
    verify:
      tags: default
post-checkout:
  scripts:
    ensure-branch-name:
      tags: default
    01-bundle-checkinstall:
      tags: default
