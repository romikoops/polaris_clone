import React from 'react'
import styles from '../QuoteCardScheduleListItem.scss'

const QuoteCardScheduleListItemPlaceholder = () => (
  <div className={`flex-100 ${styles.dates_container} ${styles.placeholder}`} />
)

export default QuoteCardScheduleListItemPlaceholder
