import React, {Component} from 'react';
import PropTypes from 'prop-types';

class RouteResult exteeds Component {
    constructor(props){
      super(props);
    }

    render() {
      return (
      
        )
    }
}
RouteResult.PropTypes = {
  theme: PropTypes.object,
  result: PropTypes.object,
  selectResult: PropTypes.func
}