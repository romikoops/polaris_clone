ci:
  required:
    - Docker
    - Sentry
    - Test / App
    - Test / Gems

dependencies:
  override: []

kubernetes:
  namespace: polaris
  context: arn:aws:eks:eu-central-1:003688427525:cluster/armada
