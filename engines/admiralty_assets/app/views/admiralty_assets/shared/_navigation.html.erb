<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">
    <%= image_tag 'admiralty_assets/logo.png', size: '30x30' %>
    Admiralty
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if defined?(admiralty_tenants) %>
        <li class="nav-item">
          <%= link_to 'Tenants', admiralty_tenants.tenants_path, class: "nav-link #{controller_namespace == 'admiralty_tenants' ? 'active' : ''}" %>
        </li>
      <% end %>
      <% if defined?(admiralty_reports) %>
        <li>
          <%= link_to 'Reports', admiralty_reports.reports_path, class: "nav-link #{controller_namespace == 'admiralty_reports' ? 'active' : ''}" %>
        </li>
      <% end %>
    </ul>
    <% if defined?(admiralty_auth) %>
      <span class="navbar-text">
        <%= link_to 'Sign Out', admiralty_auth.delete_login_url %>
      </span>
    <% end %>
  </div>
</nav>
